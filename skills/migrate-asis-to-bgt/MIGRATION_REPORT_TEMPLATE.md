# 마이그레이션 보고서: {화면명}

- **ASIS 메뉴 path / 화면코드**: `{asis_menu_path}` / `{화면코드}`
- **ASIS 경로**: `{asis_path}`
- **BGT 경로**: `{bgt_path}`
- **사용 모델**: {모델}
- **작성일**: {날짜}

---

## 1. 마이그레이션 개요

| 항목 | ASIS | TOBE(BGT) |
|------|------|-----------|
| 화면 | `{asis_menu_path}` (`{화면코드}`) | `{화면명}` (`{bgt_path}`) |
| 참고 화면(Phase 2) | — | `{참고화면 경로}` |

---

## 2. 구현 범위 및 파일 목록

**구현 요약**: {FE/BE가 구현한 화면·주요 기능 요약}

### 생성·변경 파일 (FE)

| 파일 | 구분 | 설명 |
|------|------|------|
| `{경로}` | 신규/수정 | {설명} |

### 생성·변경 파일 (BE)

| 파일 | 구분 | 설명 |
|------|------|------|
| `{경로}` | 신규/수정 | {설명} |

---

## 3. 검증 결과 요약

| 검증 단계 | 결과 | 비고 |
|----------|------|------|
| Phase 4 UI 레이아웃 | ✅ / ❌ | |
| Phase 5 개발 표준 | ✅ / ❌ | |
| Phase 6 QA | ✅ / ❌ | |

- **분석 보고서**: 같은 폴더의 `{화면명}_analysis_{날짜}.md`

### 검증 불일치 항목 (기준 대비 차이 — 요소/기대값/현재값)

각 검증 단계에서 기준(ASIS / 개발 표준)과 다르게 확인된 항목. 재구현·수정의 참고 자료다. 모두 해소됐으면 "없음".

| 검증 단계 | 요소 | 기대값 | 현재값 |
|----------|------|--------|--------|
| Phase 4 / 5 / 6 | {불일치 지점} | {ASIS·표준 기대값} | {현재 TOBE 값} |

---

## 4. 테스트 시나리오 결과

| # | 시나리오 | 결과 | 비고 |
|---|---------|------|------|
| 1 | 화면 진입 및 초기 데이터 조회 | ✅ / ❌ | |
| 2 | 검색 조건 변경 후 재조회 | ✅ / ❌ | |
| 3 | 신규 등록 | ✅ / ❌ | |
| 4 | 수정 | ✅ / ❌ | |
| 5 | 삭제 | ✅ / ❌ | |
| 6 | 권한별 버튼/탭 노출 | ✅ / ❌ | |
| 7 | 필수값 미입력 시 유효성 검사 | ✅ / ❌ | |
| ... | ... | | |

---

## 5. SP 연동·컬럼 매핑 확인

`bgt-be/docs/sp` 문서를 기준으로 SP CALL 시그니처와 컬럼 매핑이 일치하는지 확인한다.

| 파일 | 메서드 | 연동 SP | 확인 결과 |
|------|--------|---------|----------|
| `{도메인}Mapper.xml` | `{메서드명}` | `{SP_이름}` | CALL 시그니처(파라미터·커서) 일치 여부 |
| `{도메인}VO/Model` | — | `{SP_이름}` | 커서 컬럼 alias와 필드 매핑 일치 여부 |

---

## 6. ASIS 대비 차이점

### 기능 누락 / 미구현
- 없음 / {항목 목록}

### 의도적 변경 (BGT 규칙 적용)
- {예: API 호출 방식 변경 — ASIS jQuery Ajax → BGT callApi 유틸}
- {예: 그리드 컴포넌트 변경 — ASIS jqGrid → BGT IBSheet}

### 확인 필요 항목
- {사용자 승인이 필요한 변경 사항}

---

## 7. 후속 작업

- [ ] SP CALL 시그니처·컬럼 alias가 `bgt-be/docs/sp` 문서와 일치하는지 확인
- [ ] {기타 후속 작업}
